#!/usr/bin/bash

if [ "$EUID" -ne 0 ]
  then echo "Please run as root"
  exit
fi

snapper -c home create-config /var/home
vi /etc/snapper/configs/home

systemctl enable snapper-timeline.timer
systemctl enable snapper-cleanup.timer
